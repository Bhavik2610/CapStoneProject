// Placeholder file for the `components` folder.
//
// This folder holds reusable UI pieces (buttons, cards, layout, etc.).
// A common pattern is to re-export components from here so other files can
// import them cleanly, e.g. `import { PlaylistCard } from "@/components"`.
//
// Example (uncomment once you create the components):
// export { default as PlaylistCard } from "./PlaylistCard";
// export { default as TrackList } from "./TrackList";

export {};
