=====================================================================
 TASK 5 — AI-generated secure .env.local example (OpenAI + Firebase)
=====================================================================

------------------------- PROMPT USED --------------------------------

Generate a secure .env.local configuration for a Next.js (Pages Router)
app that connects to both the OpenAI API and Firebase. I use the
Firebase client SDK on the frontend and the Firebase Admin SDK on the
backend. Explain which variables must stay server-only and which are
safe to expose to the browser, and add comments. Use placeholder
values only — no real secrets.

--------------------- .env.local CONTENT RECEIVED --------------------

# =====================================================================
# .env.local  —  DO NOT COMMIT. Add ".env*.local" to your .gitignore.
# Restart the dev server after editing this file.
# =====================================================================

# ---------------------------------------------------------------------
# RULE OF THUMB
#   NEXT_PUBLIC_*  -> inlined into the browser bundle. PUBLIC. Never put
#                     a real secret here.
#   (no prefix)    -> available only in server code (API routes,
#                     getServerSideProps). Keep all real secrets here.
# ---------------------------------------------------------------------

# ============================= OpenAI ================================
# SECRET. Must stay server-only (NO NEXT_PUBLIC_ prefix). Call OpenAI
# from a Next.js API route so the key never reaches the browser.
OPENAI_API_KEY=sk-proj-REPLACE_WITH_YOUR_KEY
# Optional, only if you use an org / project scoped key:
# OPENAI_ORG_ID=org-REPLACE_ME

# ==================== Firebase (client SDK) =========================
# These web-config values are NOT secrets — they identify your project
# and are meant to ship to the browser, so NEXT_PUBLIC_ is correct.
# Real protection comes from Firebase Security Rules + App Check, not
# from hiding these values.
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyREPLACE_ME
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-app.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-app
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-app.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=000000000000
NEXT_PUBLIC_FIREBASE_APP_ID=1:000000000000:web:REPLACE_ME
# Optional (Analytics):
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-REPLACE_ME

# ================= Firebase Admin SDK (server) ======================
# SECRET service-account credentials. Server-only. NEVER NEXT_PUBLIC_.
FIREBASE_ADMIN_PROJECT_ID=your-app
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-app.iam.gserviceaccount.com
# Paste the private key on ONE line and keep the literal \n escapes,
# wrapped in double quotes:
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...REPLACE_ME...==\n-----END PRIVATE KEY-----\n"

# ---------------------------------------------------------------------
# SECURITY NOTES
# 1. .env.local is git-ignored by default in create-next-app. Keep it so.
# 2. Commit a ".env.example" with the KEYS but blank/placeholder VALUES,
#    so teammates know what to set without leaking secrets.
# 3. Never call OpenAI directly from client code — proxy through an API
#    route (e.g. src/pages/api/chat.js) so OPENAI_API_KEY stays hidden.
# 4. In production, set these in your host's env-var settings
#    (Vercel / Netlify / etc.), not in a committed file.
# ---------------------------------------------------------------------

=====================================================================
NOTE: This example was generated by an AI assistant with placeholder
values. Swap in your real values locally and keep this file private.
=====================================================================
